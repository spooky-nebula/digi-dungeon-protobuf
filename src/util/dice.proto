syntax = "proto3";

message RollRequestData {
    uint32 dieQuantity = 1;
    sint32 modifier = 2;
    uint32 dieType = 3;

    extend RollData {
        repeated int32 rolls = 4;
        int64 result = 5;
    }
}